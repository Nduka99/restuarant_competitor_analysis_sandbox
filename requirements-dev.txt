# ============================================================
# MenuForge — development / tooling dependencies
# Install after requirements.txt: pip install -r requirements-dev.txt
# ============================================================

-r requirements.txt

# ── Linting & formatting ─────────────────────────────────────
ruff>=0.4.0,<1.0             # already installed at 0.15.x

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0,<6.0

# ── Notebook tooling ─────────────────────────────────────────
jupyter>=1.0.0
nbconvert>=7.0.0

# ── Visualisation (EDA figures) ──────────────────────────────
matplotlib>=3.9.0,<4.0
seaborn>=0.13.0,<1.0

# ── Profiling ────────────────────────────────────────────────
memory-profiler>=0.61.0
